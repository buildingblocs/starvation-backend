apiVersion: v1
kind: Pod
metadata:
  name: sandbox
spec:
  containers:
    - name: sandbox
      image: bbcs/sandbox
      imagePullPolicy: Never
      resources:
        limits:
          memory: "128Mi"
          cpu: "100m"
  restartPolicy: Never
  
